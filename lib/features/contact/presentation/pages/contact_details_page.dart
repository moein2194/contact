import 'package:flutter/material.dart';

class ContactDetialsPage extends StatelessWidget {
  const ContactDetialsPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Column(),
    );
  }
}
